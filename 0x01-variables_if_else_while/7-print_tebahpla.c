#include <stdio.h>
#include <string.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int x;
	x = 122;

	while(x>=97)
	{
		if(x!=101 && 113)
		{
			putchar(x);
		}
		x--;
	}
	putchar('\n');
	return (0);
}
